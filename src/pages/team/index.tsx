//local components
import { MainLayout } from 'layouts/main'

const Teams = () => {
  const seo = { page: 'team' }

  return (
    <MainLayout seo={seo}>
      <div>team</div>
    </MainLayout>
  )
}

export default Teams
